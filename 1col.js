function printSymbolPattern(rows) {
    for (let i = 0; i < rows; i++) {
        console.log('*');
    }
}

const numberOfRows = 5; // Change this number to adjust the size of the pattern
printSymbolPattern(numberOfRows);
